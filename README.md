To download data run
```bash
bash scripts/download_data.sh 
```

In the jobs I use source $HOME/venvs/nlp2brain/bin/activate to activate a venv. Dont remember what that venv is used for but you might need to create one to run the jobs. 
